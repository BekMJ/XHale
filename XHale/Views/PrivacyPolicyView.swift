import SwiftUI

struct PrivacyPolicyView: View {
    var body: some View {
        ScrollView {
            Text("Privacy Policy Content goes here...")
                .padding()
        }
        .navigationTitle("Privacy Policy")
    }
}
